/* Preenchido por scripts/baixar-logos-colecoes.mjs.
   Enquanto estiver vazio, o app tenta o logo pela internet e, se não achar,
   usa o símbolo genérico — nada quebra. */
window.__SET_LOGOS__ = {};
